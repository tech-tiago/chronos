<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || 'A Lenda de Chronos' %></title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/public/css/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=MedievalSharp&family=Inter:wght@400;500;700;900&display=swap" rel="stylesheet">
  
  <script src="https://unpkg.com/feather-icons"></script>
</head>

<body>
  <div class="container mx-auto px-4 lg:px-6 py-4">
    
    <%- include('./partials/header') %>

    <% if (success && success.length) { %>
      <div class="ornate-panel p-3 mb-4 text-green-300"><%= success %></div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="ornate-panel p-3 mb-4 text-red-300"><%= error %></div>
    <% } %>

    <main class="grid lg:grid-cols-12 gap-8">
      <%- body %>
    </main>

    <%- include('./partials/footer') %>

  </div>

  <script>
    feather.replace();
  </script>
</body>
</html>